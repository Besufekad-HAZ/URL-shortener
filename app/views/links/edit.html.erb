<h1>Edit Link</h1>

<%= render partial: "form", locals: { link: @link } %>

<div class="flex gap-2 item-center">
<%= link_to "Cancel", link_path(@link) %>
or <%= button_to "Delete", @link, method: :delete, data: { turbo_confirm: "Are you sure?" } %>
</div>
